{
  "name": "app-scholar",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema acadêmico com backend, frontend web e aplicativo mobile em Expo.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:mobile": "npm run dev --prefix mobile",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:reset": "npm run db:reset --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}